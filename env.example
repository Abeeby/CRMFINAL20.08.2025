# Configuration de synchronisation avec le VPS
# Copiez ce fichier en .env et remplissez vos informations

# === CONFIGURATION VPS ===
VPS_HOST=148.230.105.25
VPS_PORT=22
VPS_USER=root
VPS_PASSWORD=Miser1597532684.
# OU utilisez une clé SSH (recommandé)
VPS_SSH_KEY=~/.ssh/id_rsa

# Base de données du VPS
# Format MySQL: mysql://user:password@host:port/database
# Format PostgreSQL: postgresql://user:password@host:port/database
VPS_DATABASE_URL=mysql://crm_user:password@localhost:3306/crm_database

# API du VPS (si disponible)
VPS_API_URL=https://votre-vps.com/api
VPS_API_KEY=votre_cle_api_secrete

# === OPTIONS DE SYNCHRONISATION ===
# Mode: push (local vers VPS), pull (VPS vers local), bidirectional
SYNC_MODE=bidirectional

# Intervalle de sync en secondes (300 = 5 minutes)
SYNC_INTERVAL=300

# Synchronisation automatique (true/false)
AUTO_SYNC=true

# Résolution des conflits: latest, vps_priority, local_priority
CONFLICT_RESOLUTION=latest
